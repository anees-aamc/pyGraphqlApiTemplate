# Local development example
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/mydb

# AWS env vars (injected at runtime, not stored in .env)
# AWS_SECRET_NAME=myapp/database_url
# AWS_REGION=us-east-1